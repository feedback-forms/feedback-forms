name: CI/CD Pipeline

on:
  push:
    branches: [ main, feat/deployment ]

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit

  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yml
    secrets: inherit